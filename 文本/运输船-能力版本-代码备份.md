# 运输船代码备份（当前版本）

**备份时间**: 2026-06-18

---

## 文件 1: TransportShip.cs

**路径**: `RedAlert2ModCode/Allies/Cards/TransportShip.cs`

```csharp
#nullable enable

using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using Godot;
using MegaCrit.Sts2.Core.CardSelection;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.GameActions.Multiplayer;
using MegaCrit.Sts2.Core.Localization.DynamicVars;
using MegaCrit.Sts2.Core.Localization;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Cards;
using MegaCrit.Sts2.Core.Nodes.Combat;
using MegaCrit.Sts2.Core.ValueProps;
using MegaCrit.Sts2.Core.HoverTips;
using RedAlert2ModCode.Allies.Powers;
using RedAlert2ModCode.Utils;

namespace RedAlert2ModCode.Allies.Cards;

/// <summary>
/// 运输船 - 盟军海军单位卡
/// 1费技能卡，可存储手牌中的卡牌（与 TransportShipPower 绑定）
/// </summary>
public sealed class TransportShip : CardModel
{
    private static readonly CardValueStore.CardValues Values = AlliesCardValues.TransportShip;

    public TransportShip() : base((int)Values.Cost, CardType.Skill, CardRarity.Token, TargetType.Self) { }

    public override string PortraitPath => $"res://RedAlert2ModResources/images/packed/card_portraits/allies/landicon.png";

    /// <summary>
    /// 使用原版"保留"词条
    /// </summary>
    public override IEnumerable<CardKeyword> CanonicalKeywords => new[] { CardKeyword.Retain };

    protected override List<DynamicVar> CanonicalVars => new()
    {
        new IntVar("StoreCount", IsUpgraded ? Values.MagicNumber + Values.MagicNumberUpgraded : Values.MagicNumber),
        new IntVar("DollarNumber", Values.DollarValue)
    };

    protected override IEnumerable<IHoverTip> ExtraHoverTips =>
    [
        ModCardKeywords.Navy.CreateHoverTip()
    ];

    protected override async Task OnPlay(PlayerChoiceContext choiceContext, CardPlay cardPlay)
    {
        GD.Print($"[TransportShip] OnPlay 被调用 - IsUpgraded={IsUpgraded}");

        // 获取或创建运输船能力
        var transportShipPower = Owner.Creature.GetPower<TransportShipPower>();
        if (transportShipPower == null)
        {
            GD.Print("[TransportShip] 创建新的 TransportShipPower");
            transportShipPower = await PowerCmd.Apply<TransportShipPower>(Owner.Creature, 1m, Owner.Creature, this);
        }

        // 检查能力中是否有存储的卡牌
        if (!transportShipPower.HasStoredCards)
        {
            await StoreCards(choiceContext, transportShipPower);
        }
        else
        {
            await transportShipPower.ReleaseCards();
        }
    }

    private async Task StoreCards(PlayerChoiceContext choiceContext, TransportShipPower power)
    {
        // 获取可存储的最大数量（升级后5张，基础3张）
        int countToStore = IsUpgraded ? Values.MagicNumber + Values.MagicNumberUpgraded : Values.MagicNumber;
        
        GD.Print($"[TransportShip] 准备存储最多 {countToStore} 张卡牌");

        // 允许选择0到countToStore张卡牌，使用自定义提示
        var selectPrompt = new LocString("cards", "TRANSPORT_SHIP.select_prompt");
        selectPrompt.Add("0", 0);
        selectPrompt.Add("1", countToStore);
        var prefs = new CardSelectorPrefs(selectPrompt, 0, countToStore)
        {
            RequireManualConfirmation = true
        };

        var selectedCards = (await CardSelectCmd.FromHand(
            choiceContext,
            base.Owner,
            prefs,
            c => c != this,
            this
        )).ToList();

        if (selectedCards.Count == 0)
        {
            GD.Print("[TransportShip] 玩家选择0张卡牌");
            return;
        }

        GD.Print($"[TransportShip] 玩家选择了 {selectedCards.Count} 张卡牌进行存储");

        // 使用能力存储卡牌
        await power.StoreCards(selectedCards);
        GD.Print($"[TransportShip] 存储完成，已存储 {selectedCards.Count} 张卡牌");
    }

    protected override void OnUpgrade()
    {
        // 升级后存储数量从3张增加到5张
        DynamicVars["StoreCount"].UpgradeValueBy(Values.MagicNumberUpgraded);
    }
}
```

---

## 文件 2: TransportShipPower.cs

**路径**: `RedAlert2ModCode/Allies/Powers/TransportShipPower.cs`

```csharp
using System.Collections.Generic;
using System.Threading.Tasks;
using Godot;
using MegaCrit.Sts2.Core.Commands;
using MegaCrit.Sts2.Core.Entities.Cards;
using MegaCrit.Sts2.Core.Localization;
using MegaCrit.Sts2.Core.Models;
using MegaCrit.Sts2.Core.Models.Cards;
using MegaCrit.Sts2.Core.Models.Powers;
using MegaCrit.Sts2.Core.Entities.Powers;
using MegaCrit.Sts2.Core.Nodes.Combat;

namespace RedAlert2ModCode.Allies.Powers;

/// <summary>
/// 运输船能力
/// 用于存储和释放卡牌，并显示当前存储的卡牌信息
/// </summary>
public sealed class TransportShipPower : PowerModel
{
    public override PowerType Type => PowerType.Buff;

    public override PowerStackType StackType => PowerStackType.Single;

    public override bool IsInstanced => true;

    /// <summary>
    /// 存储的卡牌列表
    /// </summary>
    private List<CardModel> _storedCards = new();

    /// <summary>
    /// 是否有存储的卡牌
    /// </summary>
    public bool HasStoredCards => _storedCards.Count > 0;

    /// <summary>
    /// 存储的卡牌数量
    /// </summary>
    public int StoredCount => _storedCards.Count;

    /// <summary>
    /// 本地化描述，显示当前存储的卡牌列表
    /// </summary>
    public override LocString Description
    {
        get
        {
            var locString = new LocString("powers", base.Id.Entry + ".description");
            
            // 添加存储的卡牌名称（确保键名与本地化文件中的占位符匹配）
            for (int i = 0; i < _storedCards.Count && i < 10; i++)
            {
                locString.Add($"StoredCard{i}", _storedCards[i].Title.ToString());
            }
            
            // 添加存储数量
            locString.Add("StoredCount", _storedCards.Count);
            
            return locString;
        }
    }

    /// <summary>
    /// 存储卡牌（从手牌中移除并存储）
    /// </summary>
    public async Task StoreCards(List<CardModel> cards)
    {
        foreach (var card in cards)
        {
            // 从手牌 UI 中移除卡牌
            NPlayerHand.Instance?.Remove(card);
            // 从数据层移除，但不标记为消耗
            card.RemoveFromCurrentPile();
            // 存储卡牌对象
            _storedCards.Add(card);
            
            GD.Print($"[TransportShipPower] 卡牌 {card.Id.Entry} ({card.Title}) 已从手牌移除并存储");
        }
    }

    /// <summary>
    /// 释放所有存储的卡牌到手牌
    /// </summary>
    public async Task ReleaseCards()
    {
        if (!HasStoredCards) return;

        GD.Print($"[TransportShipPower] 释放存储的卡牌，数量: {_storedCards.Count}");

        foreach (var card in _storedCards)
        {
            // 将卡牌添加回玩家手牌底部
            await CardPileCmd.Add(card, PileType.Hand, CardPilePosition.Bottom, this);
            GD.Print($"[TransportShipPower] 卡牌 {card.Id.Entry} ({card.Title}) 已释放到手牌");
        }

        // 清空存储列表
        _storedCards.Clear();
        GD.Print($"[TransportShipPower] 所有存储的卡牌已释放");
    }
}
```

---

## 相关本地化配置

### cards.json
```json
"TRANSPORT_SHIP.title": "运输船",
"TRANSPORT_SHIP.description": "选择手牌中任意 {StoreCount} 张卡牌存储起来。若已存储卡牌，则释放全部到手牌。",
"TRANSPORT_SHIP.select_prompt": "选择 {0}-{1} 张牌来存储。"
```

### powers.json
```json
"TRANSPORT_SHIP_POWER.title": "运输船",
"TRANSPORT_SHIP_POWER.description": "当前存储卡牌：\n{StoredCard0}\n{StoredCard1}\n{StoredCard2}\n{StoredCard3}\n{StoredCard4}\n{StoredCard5}\n{StoredCard6}\n{StoredCard7}\n{StoredCard8}\n{StoredCard9}"
```

---

## 数值定义 (AlliesCardValues.cs)

```csharp
public static CardValueStore.CardValues TransportShip => new()
{
    Cost = 1,
    MagicNumber = 3,           // 基础存储数量
    MagicNumberUpgraded = 2,   // 升级后增加数量
    DollarValue = 900
};
```
