# 角色阵营配置 & 自定义初始卡组 — 技术实现文档

> 目标项目：`d:\RedAlert2Project\red-alert-2-mod\`（红警2 Mod，ID=`RedAlert2Mod`，基于 RitsuLib）
> 参考对象：`D:\RedAlert2Project\mods\其他mod\控制台\FreeLoadout-master\`（控制台 Mod）
> 编写日期：2026-07-30

---

## 0. 需求摘要

### 功能一：角色阵营配置
- 新增设置：获取当前游戏**全部角色**，可为每个角色选择阵营（无 / 盟军 / 苏军 / 尤里），**默认无**。
- 存储方式：学习海克斯 Mod / FreeLoadout 的 JSON 文件存储。
- 效果：某角色配置了非"无"阵营后，该角色**开局**会：
  1. 触发对应阵营的**国旗事件**；
  2. 获得对应阵营的**一张基地车（MCV）卡牌**；
  3. 获得初始遗物**刀乐**（但本 Mod 角色已有刀乐，**不重复赋予**）。
- **例外**：盟军角色配盟军、苏军角色配苏军、尤里角色配尤里 → 等同无效果（已有基地车卡牌和国旗事件）。

### 功能二：自定义初始卡组
- 获取当前游戏**全部角色**的初始卡组，展示、编辑，进入游戏时使用编辑后的自定义卡组。

---

## 1. 现状分析（关键代码定位）

### 1.1 角色定义

本项目当前有 2 个 RA2 角色（Yuri 目录仅有卡牌注册，无角色类）：

| 角色 | 文件 | 基类 | CharacterId |
|------|------|------|-------------|
| 盟军 | [AlliesCharacter.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Allies/AlliesCharacter.cs) | `ModCharacterTemplate<AlliesCardPool, AlliesRelicPool, AlliesPotionPool>` | `Allies` |
| 苏军 | [SovietCharacter.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Soviet/SovietCharacter.cs) | `ModCharacterTemplate<SovietCardPool, SovietRelicPool, SovietPotionPool>` | `Soviet` |

初始卡组与遗物均通过 RitsuLib 的虚属性声明：

```csharp
// AlliesCharacter.cs
protected override IEnumerable<StartingDeckEntry> StartingDeckEntries => new[]
{
    StartingDeckEntry.Of<AmericanSoldier>(5),
    StartingDeckEntry.Of<GrizzlyTank>(5),
    StartingDeckEntry.Of<AlliedMCV>(1),        // ← 基地车已内置
    StartingDeckEntry.Of<AlliedWallCard>(1),
};

protected override IEnumerable<Type> StartingRelicTypes => new[]
{
    typeof(RedAlert2ModCode.Common.Relics.DollarRelic),  // ← 刀乐已内置
};
```

> **结论**：RA2 角色的 MCV 和刀乐是通过 `StartingDeckEntries` / `StartingRelicTypes` 在角色定义级内置的，开局自动发放。

### 1.2 阵营检测与国旗事件

[FlagManager.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Common/Utils/FlagManager.cs) 通过角色 ID 字符串匹配判定阵营：

```csharp
public static Faction GetPlayerFaction(Player player)
{
    string? charId = player.Character?.Id?.Entry;
    if (charId == null) return Faction.None;
    if (charId.Equals("Allies", ...) || charId.Contains("ALLIES", ...)) return Faction.Allies;
    if (charId.Equals("Soviet", ...) || charId.Contains("SOVIET", ...)) return Faction.Soviet;
    if (charId.Contains("YURI", ...)) return Faction.Yuri;
    return Faction.None;   // ← 原版角色（Ironclad/Silent 等）走这里
}
```

[FlagSelectionPatches.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Common/Patches/FlagSelectionPatches.cs) Patch `NGame.StartRun`，开局后：
- `Faction.None` → 跳过（原版角色无国旗事件）
- `Faction.Yuri` → 自动授予尤里国旗
- `Faction.Allies`/`Soviet` → 弹出国旗选择界面（单机/联机均支持）

### 1.3 基地车卡牌

| 阵营 | 卡牌类 | 文件 |
|------|--------|------|
| 盟军 | `AlliedMCV` | [Allies/Cards/AlliedMCV.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Allies/Cards/AlliedMCV.cs) |
| 苏军 | `SovietMCV` | [Soviet/Cards/SovietMCV.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Soviet/Cards/SovietMCV.cs) |
| 尤里 | ⚠️ 待确认 | `Yuri/YuriCardRegistry.cs` 内注册（需核实是否有 `YuriMCV` 卡牌类） |

MCV 卡牌特性：0 费技能卡、`CanonicalKeywords = { Innate }`（固有）、打出后从科技线解锁建筑中选一张加入手牌。

### 1.4 刀乐遗物

[Common/Relics/DollarRelic.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Common/Relics/DollarRelic.cs) — 已作为 RA2 角色的 `StartingRelicTypes`。

### 1.5 现有配置存储范式

[DollarTransferConfig.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Common/Utils/DollarTransferConfig.cs) 已建立本项目配置存储惯例：

```csharp
// 路径：res://RedAlert2ModResources/config/xxx.json
string configPath = Path.Combine(
    ProjectSettings.GlobalizePath("res://RedAlert2ModResources/config"),
    "dollar_transfer.json");
// 单例 Instance + 懒加载 + Godot.Json 解析 + SaveConfig() 回写
```

### 1.6 Mod 入口与补丁安装

[ModInitializer.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/ModInitializer.cs)：
- `harmony.PatchAll()` 自动应用所有 `[HarmonyPatch]`
- 另有若干 `XxxPatches.Install(harmony)` 手动安装

### 1.7 本地化

4 语言：`RedAlert2Mod/localization/{zhs,eng,jpn,kor}/`，含 `cards.json`/`powers.json`/`relics.json`/`events.json`/`characters.json` 等。

---

## 2. 功能一：角色阵营配置 — 实现设计

### 2.1 文件结构（新增）

```
RedAlert2ModCode/
└── Common/
    ├── Utils/
    │   └── CharacterFactionConfig.cs      # ★ 阵营配置存储（单例 JSON）
    ├── Patches/
    │   └── CharacterFactionPatch.cs       # ★ 开局注入 MCV/刀乐/国旗事件
    └── UI/
        └── FactionConfigPanel.cs          # ★ 配置面板 UI（热键唤出）
RedAlert2ModResources/config/
    └── character_faction.json             # ★ 运行时生成
RedAlert2Mod/localization/{zhs,eng,jpn,kor}/
    └── ui.json (或 faction_config.json)   # ★ 面板文案
```

### 2.2 配置存储类 `CharacterFactionConfig`

仿照 `DollarTransferConfig` + FreeLoadout `Config.cs`：

```csharp
// RedAlert2ModCode/Common/Utils/CharacterFactionConfig.cs
using Godot;
using System.Collections.Generic;
using System.IO;

namespace RedAlert2ModCode.Common.Utils;

public class CharacterFactionConfig
{
    /// <summary>角色 CharacterId → 阵营字符串（"None"/"Allies"/"Soviet"/"Yuri"）</summary>
    public Dictionary<string, string> CharacterFactions { get; set; } = new();

    private static CharacterFactionConfig? _instance;
    public static CharacterFactionConfig Instance => _instance ??= Load();

    private const string FileName = "character_faction.json";

    public FlagManager.Faction GetFaction(string characterId)
    {
        if (CharacterFactions.TryGetValue(characterId, out var f)
            && System.Enum.TryParse<FlagManager.Faction>(f, out var faction))
            return faction;
        return FlagManager.Faction.None;   // 默认无
    }

    public void SetFaction(string characterId, FlagManager.Faction faction)
    {
        CharacterFactions[characterId] = faction.ToString();
        Save();
    }

    private static CharacterFactionConfig Load() { /* 同 DollarTransferConfig.LoadConfig */ }
    public void Save() { /* 同 DollarTransferConfig.SaveConfig，写到 config/character_faction.json */ }
}
```

配置文件示例：
```json
{
  "character_factions": {
    "Ironclad": "Allies",
    "Silent":   "Yuri"
  }
}
```

### 2.3 阵营解析逻辑（复用 + 扩展 FlagManager）

新增方法，**不改动** `GetPlayerFaction`（保持原语义），新增"有效阵营"概念：

```csharp
// FlagManager.cs 追加
public static Faction GetEffectiveFaction(Player player)
{
    string? charId = player.Character?.Id?.Entry;
    if (charId == null) return Faction.None;

    // 1. 优先读配置覆盖（原版角色走这里）
    Faction configFaction = CharacterFactionConfig.Instance.GetFaction(charId);
    if (configFaction != Faction.None) return configFaction;

    // 2. 回退到角色原生阵营检测（RA2 角色走这里）
    return GetPlayerFaction(player);
}
```

**关键判定：是否需要注入 MCV/刀乐**

```csharp
// 是否是"配置赋予的"阵营（即非角色原生）——只有这种情况才需要注入 MCV/刀乐
public static bool IsFactionFromConfig(Player player)
{
    string? charId = player.Character?.Id?.Entry;
    if (charId == null) return false;
    Faction configFaction = CharacterFactionConfig.Instance.GetFaction(charId);
    if (configFaction == Faction.None) return false;
    Faction nativeFaction = GetPlayerFaction(player);   // 原生检测
    // 配置阵营与原生阵营相同 → 等同无效果（需求中的例外）
    return configFaction != nativeFaction;
}
```

> 这正对应需求："盟军配置选中盟军…则等同于无效果"。

### 2.4 开局注入补丁 `CharacterFactionPatch`

复用 `FlagSelectionPatches` 已 Patch 的 `NGame.StartRun` 时机。有两种集成方式：

**方式 A（推荐）：扩展现有 `FlagSelectionPatches`**

在 `EnsureFlagSelected` 中把 `GetPlayerFaction` 改为 `GetEffectiveFaction`，并在其中追加 MCV/刀乐注入。这样国旗事件复用现有单机/联机逻辑。

**方式 B：独立 Patch**（与 FlagSelectionPatches 串联，注意顺序）

```csharp
// RedAlert2ModCode/Common/Patches/CharacterFactionPatch.cs
[HarmonyPatch(typeof(NGame), "StartRun")]
public static class CharacterFactionPatch
{
    private static async void Postfix(RunState runState, ref Task __result)
    {
        await __result;   // 等原始 StartRun 完成（含 FlagSelectionPatches 的国旗流程）
        await GrantConfigFactionRewards(runState);
    }

    private static async Task GrantConfigFactionRewards(RunState runState)
    {
        foreach (Player player in runState.Players)
        {
            if (!FlagManager.IsFactionFromConfig(player)) continue;

            FlagManager.Faction faction = FlagManager.GetEffectiveFaction(player);

            // 1. 注入基地车卡牌到牌组
            await GrantMcvCard(player, faction);

            // 2. 注入刀乐遗物（若未拥有）
            GrantDollarRelic(player);

            // 国旗事件由 FlagSelectionPatches 用 GetEffectiveFaction 触发（方式A），
            // 或在此处手动触发（方式B，需调用 EnsureFlagSelected 逻辑）。
        }
    }

    private static async Task GrantMcvCard(Player player, FlagManager.Faction faction)
    {
        System.Type mcvType = faction switch
        {
            FlagManager.Faction.Allies => typeof(Allies.Cards.AlliedMCV),
            FlagManager.Faction.Soviet => typeof(Soviet.Cards.SovietMCV),
            FlagManager.Faction.Yuri   => /* ⚠️ 核实 YuriMCV 类型 */,
            _ => null!
        };
        if (mcvType == null) return;

        var template = ModelDb.Card(mcvType);          // 取卡牌模板
        var card = runState.CreateCard(template, player);
        await CardPileCmd.Add(card, PileType.Deck);    // 加入牌组
    }

    private static void GrantDollarRelic(Player player)
    {
        bool hasDollar = player.Relics.Any(r => r is Common.Relics.DollarRelic);
        if (hasDollar) return;                          // 已有刀乐，不重复赋予
        var relic = ModelDb.Relic<Common.Relics.DollarRelic>().ToMutable();
        TaskHelper.RunSafely(RelicCmd.Obtain(relic, player));
    }
}
```

> ⚠️ **注意**：`FlagSelectionPatches.StartRunPostfix` 用 `_ = StartRunAfterOriginal(__result, runState)` 启动了独立任务。若方式 B 也用 `await __result`，两个 Postfix 会各自等待原任务，互不干扰。但需确认国旗事件在"配置阵营"下能触发——建议采用**方式 A**，直接把 `EnsureFlagSelected` 里的 `GetPlayerFaction` 换成 `GetEffectiveFaction`，这样国旗事件天然复用，无需重复实现。

### 2.5 方式 A 的最小改动

[FlagSelectionPatches.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/Common/Patches/FlagSelectionPatches.cs) `EnsureFlagSelected` 内：

```diff
- FlagManager.Faction faction = FlagManager.GetPlayerFaction(player);
+ FlagManager.Faction faction = FlagManager.GetEffectiveFaction(player);
  if (faction == FlagManager.Faction.None) return false;
+ // 配置赋予的阵营需要额外注入 MCV/刀乐
+ if (FlagManager.IsFactionFromConfig(player))
+ {
+     await GrantMcvCard(player, faction);   // 见 2.4
+     GrantDollarRelic(player);
+ }
```

### 2.6 配置面板 UI `FactionConfigPanel`

仿 FreeLoadout `LoadoutPanel` 的 CanvasLayer 面板：

```csharp
// RedAlert2ModCode/Common/UI/FactionConfigPanel.cs
internal static class FactionConfigPanel
{
    private static CanvasLayer? _layer;

    internal static void Toggle() { /* 若未构建则 Build，否则切换 Visible */ }

    private static void Build()
    {
        _layer = new CanvasLayer { Layer = 100 };
        // 半透明 backstop + PanelContainer + VBoxContainer（仿 LoadoutPanel.Build）

        // 标题：角色阵营配置

        // 遍历全部角色
        foreach (var character in ModelDb.Characters)   // ← 获取全部角色
        {
            string charId = character.Id.Entry;
            FlagManager.Faction current = CharacterFactionConfig.Instance.GetFaction(charId);

            // 一行：[角色名] [无] [盟军] [苏军] [尤里]（当前阵营高亮）
            // 点击按钮 → CharacterFactionConfig.Instance.SetFaction(charId, faction) → 刷新
        }

        // [关闭] [保存] 按钮
        NGame.Instance?.AddChild(_layer);
    }
}
```

**获取全部角色**：`ModelDb.Characters`（游戏核心 API，返回 `IReadOnlyList<CharacterModel>`，⚠️ 上线前需核实属性名，可能是 `ModelDb.Characters` 或 `ModelDb.DebugCharacters`）。

**热键唤出**：仿 FreeLoadout `NGameInputPatch`，Patch `NGame._Input`：

```csharp
[HarmonyPatch(typeof(NGame), nameof(NGame._Input))]
internal static class FactionConfigHotkeyPatch
{
    private static bool Prefix(NGame __instance, InputEvent ev)
    {
        if (ev is InputEventKey { Pressed: true, Keycode: Key.F6 })  // 例如 F6
        {
            __instance.GetViewport()?.SetInputAsHandled();
            FactionConfigPanel.Toggle();
            return false;
        }
        return true;
    }
}
```

> 热键选择需避开 FreeLoadout 的 F1、BonModConfig 的 F10。建议 F6（或集成到顶栏按钮，仿 `TopBarTrainerButtonPatch`）。

### 2.7 数据流总览

```
玩家配置阶段（主菜单/任意时刻按 F6）
  FactionConfigPanel 列出 ModelDb.Characters
  → 玩家为某原版角色选"盟军"
  → CharacterFactionConfig.SetFaction("Ironclad", Allies)
  → 写入 character_faction.json

开局阶段（NGame.StartRun）
  FlagSelectionPatches.EnsureFlagSelected(player)
  → faction = GetEffectiveFaction(player)   // 读配置 → Allies
  → IsFactionFromConfig? true（原版角色≠原生 Allies）
  → GrantMcvCard(player, Allies)            // 注入 AlliedMCV 到牌组
  → GrantDollarRelic(player)                // 注入刀乐（若未有）
  → 弹出国旗选择界面（盟军5国旗选1）        // 复用现有逻辑
```

### 2.8 边界与风险

| 风险 | 应对 |
|------|------|
| 原版角色没有 RA2 建筑科技线，MCV 打出后可能选不到建筑 | MCV 内部用 `CreateTechTreeFromDeck` + `AlliedTechTreeConfig`，原版角色无 RA2 建筑卡时可能空列表。需测试 MCV 在原版角色上的行为，必要时给原版角色 MCV 时附带一张初始建筑（如发电厂）。 |
| 尤里阵营无国旗选择（自动授予），但需确认是否有尤里 MCV | 核实 `YuriCardRegistry.cs` 是否注册 `YuriMCV`；若无，尤里配置仅授予国旗+刀乐，不授予 MCV（或新增 YuriMCV 卡牌）。 |
| 联机同步 | MCV 卡牌/刀乐注入须对所有玩家执行（仿 `TrainerState.Apply` 遍历 `runState.Players`）；国旗事件已有联机同步逻辑（`EnsureFlagsSelectedMultiplayer`），改用 `GetEffectiveFaction` 即可。 |
| 配置文件被删/损坏 | 单例 Load 容错，返回默认（全 None），不崩溃。 |
| 与 RitsuLib `[RegisterCharacter]` 兼容 | 不改动角色注册，仅在 run 开始后注入，与 RitsuLib 无冲突。 |
| 重复注入（重开同一 run） | `GrantDollarRelic` 已查重；MCV 注入应检查牌组是否已有该 MCV（`player.Deck.Cards.Any(c => c is AlliedMCV)`）。 |

---

## 3. 功能二：自定义初始卡组 — 实现设计

### 3.1 与 FreeLoadout 的关键区别

> ⚠️ FreeLoadout 的 `CharacterTab`/`CardEditPanel` 是**运行中**修改当前牌堆，**不能**直接用于"开局前编辑初始卡组"。本功能需自研：
> - **展示**：在 run 开始前读取角色默认初始卡组并渲染；
> - **编辑**：让玩家增删卡牌、调数量；
> - **生效**：在 run 开始时用自定义卡组**替换**默认初始卡组。

### 3.2 文件结构（新增）

```
RedAlert2ModCode/
└── Common/
    ├── Utils/
    │   └── CharacterDeckConfig.cs        # ★ 卡组配置存储
    ├── Patches/
    │   └── StartingDeckOverridePatch.cs  # ★ 开局替换初始卡组
    └── UI/
        └── DeckEditorPanel.cs            # ★ 卡组编辑面板 UI
RedAlert2ModResources/config/
    └── character_decks.json              # ★ 运行时生成
```

### 3.3 数据模型

```csharp
public class DeckEntry
{
    public string CardId { get; set; } = "";       // 卡牌 ModelId.Entry
    public int Count { get; set; } = 1;
    public int UpgradeLevel { get; set; } = 0;
}

public class CharacterDeckConfig
{
    /// <summary>角色 CharacterId → 自定义卡组</summary>
    public Dictionary<string, List<DeckEntry>> CustomDecks { get; set; } = new();

    private static CharacterDeckConfig? _instance;
    public static CharacterDeckConfig Instance => _instance ??= Load();

    public bool HasCustomDeck(string charId) => CustomDecks.ContainsKey(charId);

    public List<DeckEntry> GetCustomDeck(string charId)
        => CustomDecks.TryGetValue(charId, out var d) ? d : new();

    public void SetCustomDeck(string charId, List<DeckEntry> deck) { CustomDecks[charId] = deck; Save(); }
    public void ClearCustomDeck(string charId) { CustomDecks.Remove(charId); Save(); }

    // Load()/Save() 同 DollarTransferConfig，路径 config/character_decks.json
}
```

### 3.4 读取角色默认初始卡组（用于展示）

不同角色来源不同，需分别处理：

```csharp
// 获取某角色的默认初始卡组（只读展示用）
public static List<DeckEntry> GetDefaultDeck(CharacterModel character)
{
    var result = new List<DeckEntry>();
    string charId = character.Id.Entry;

    // 情况1：RA2 角色（继承 ModCharacterTemplate<>）—— 反射读 StartingDeckEntries
    var t = character.GetType();
    var prop = t.GetProperty("StartingDeckEntries",
        System.Reflection.BindingFlags.NonPublic | System.Reflection.BindingFlags.Instance
        | System.Reflection.BindingFlags.FlattenHierarchy);
    if (prop != null && prop.GetValue(character) is IEnumerable<StartingDeckEntry> entries)
    {
        foreach (var e in entries)
        {
            // StartingDeckEntry 含 CardType / Count（具体字段名⚠️需核实）
            result.Add(new DeckEntry { CardId = e.CardType.Name, Count = e.Count });
        }
        return result;
    }

    // 情况2：原版角色 —— 调用 CharacterModel 的初始卡组生成
    // ⚠️ API 待核实：可能是 character.StartingDeck / CreateStartingDeck() / ModelDb 内置
    // 兜底：返回空，提示"该角色初始卡组需进入游戏后查看"
    return result;
}
```

> ⚠️ `StartingDeckEntry` 的字段名（`CardType`/`Count`/`UpgradeLevel`）需在 RitsuLib 的 `STS2-RitsuLib.xml` 或源码中核实。可读 `d:\RedAlert2Project\red-alert-2-mod\RestuLib\lib\0.109.0\STS2-RitsuLib.xml` 确认。

### 3.5 开局替换补丁 `StartingDeckOverridePatch`

**核心时机**：Patch `NGame.StartRun`，在 run 创建完成、初始卡组已生成后替换。与 `FlagSelectionPatches` 同一时机，注意两者都在 `await __result` 之后执行。

```csharp
[HarmonyPatch(typeof(NGame), "StartRun")]
public static class StartingDeckOverridePatch
{
    private static async void Postfix(RunState runState, ref Task __result)
    {
        await __result;
        await OverrideStartingDeck(runState);
    }

    private static async Task OverrideStartingDeck(RunState runState)
    {
        foreach (Player player in runState.Players)
        {
            string? charId = player.Character?.Id?.Entry;
            if (charId == null) continue;
            if (!CharacterDeckConfig.Instance.HasCustomDeck(charId)) continue;

            var customDeck = CharacterDeckConfig.Instance.GetCustomDeck(charId);

            // 1. 清空当前初始牌组（保留由功能一注入的 MCV 等？需决策，见 3.7）
            var existing = player.Deck.Cards.ToList();
            foreach (var c in existing)
                await CardPileCmd.RemoveFromDeck(c, showPreview: false);

            // 2. 按配置添加卡牌
            foreach (var entry in customDeck)
            {
                if (!TryGetCardTemplate(entry.CardId, out var template)) continue;
                for (int i = 0; i < entry.Count; i++)
                {
                    var card = runState.CreateCard(template, player);
                    for (int u = 0; u < entry.UpgradeLevel; u++)
                        CardCmd.Upgrade(card);
                    await CardPileCmd.Add(card, PileType.Deck);
                }
            }
        }
    }

    private static bool TryGetCardTemplate(string cardId, out CardModel template)
    {
        // 通过 ModelDb 按 Entry ID 查找卡牌模板
        // ⚠️ API 待核实：ModelDb.GetByIdOrNull<CardModel>(ModelId) 或遍历卡池
        template = null!;
        // ... 实现
        return template != null;
    }
}
```

> **替代方案（更稳）**：若 `NGame.StartRun` 后替换牌组有联机 checksum 问题，可改为 Patch `RunState` 的初始牌组生成方法（如 `RunState.InitializeDeck` / `Player.CreateStartingDeck`），在生成阶段直接替换 `StartingDeckEntries`。这需要在 RitsuLib `ModCharacterTemplate` 中找到调用 `StartingDeckEntries` 的虚方法并 Prefix 拦截。建议先试 `StartRun` Postfix，联机出问题再下沉。

### 3.6 卡组编辑面板 UI `DeckEditorPanel`

仿 FreeLoadout `CardEditPanel`，左侧角色列表 + 右侧卡组编辑：

```
CanvasLayer (Layer=100)
└── PanelContainer
    └── VBoxContainer
        ├── HBoxContainer [标题: 自定义初始卡组] [关闭]
        ├── HBoxContainer
        │   ├── VBoxContainer (左:角色列表)
        │   │   ├── Button Ironclad    (有自定义卡组时显示✓)
        │   │   ├── Button Silent
        │   │   ├── Button Allies
        │   │   └── Button Soviet
        │   └── VBoxContainer (右:当前角色的卡组)
        │       ├── ScrollContainer
        │       │   └── VBoxContainer
        │       │       ├── [卡牌名] [×N] [-][+] [升级▼] [删除]
        │       │       ├── ...
        │       │       └── [默认卡组] 区块（只读展示 GetDefaultDeck）
        │       └── HBoxContainer [添加卡牌▼] [恢复默认] [保存]
        └── [说明文字]
```

**添加卡牌**：弹出卡牌选择器，复用 `NCardLibrary`（仿 FreeLoadout 嵌入式方案）或自绘卡池网格（按 `AlliesCardPool.AllCards` + 原版卡池枚举）。

**升级等级**：0/1/2 三档（`CardCmd.Upgrade` 在 run 内才有效，配置里只存 `UpgradeLevel` 数值，开局注入时循环升级）。

### 3.7 功能一与功能二的交互

**关键决策**：当某原版角色同时配置了"阵营=盟军"和"自定义卡组"时，注入的 MCV 是否要保留？

建议策略：
- **自定义卡组优先**：若玩家为该角色编辑了自定义卡组，视为玩家完全接管初始卡组，功能一的 MCV **不自动注入**（玩家可在编辑器里手动加入 MCV）。
- 刀乐遗物仍注入（遗物不属卡组）。
- 国旗事件仍触发。

实现：`GrantMcvCard` 调用前检查 `CharacterDeckConfig.Instance.HasCustomDeck(charId)`，若有则跳过 MCV 注入并提示。

### 3.8 数据流总览

```
玩家配置阶段（F7 唤出卡组编辑器）
  DeckEditorPanel 列出 ModelDb.Characters
  → 选中 Ironclad，右侧显示 GetDefaultDeck(Ironclad)（只读参考）
  → 玩家编辑：删 5 张打击，加 3 张 AlliedMCV，存 1 张升级
  → CharacterDeckConfig.SetCustomDeck("Ironclad", [...])
  → 写入 character_decks.json

开局阶段（NGame.StartRun）
  StartingDeckOverridePatch.OverrideStartingDeck(runState)
  → 读取 character_decks.json["Ironclad"]
  → 清空 player.Deck，按配置逐张 CreateCard + Add
  → （若该角色也配了阵营，MCV 注入被跳过，刀乐仍注入）
```

---

## 4. 本地化

新增面板文案需同步 4 语言。建议新建 `RedAlert2Mod/localization/{zhs,eng,jpn,kor}/ui.json`（若已有 ui.json 则追加）：

```json
{
  "faction_config.title": "角色阵营配置",
  "faction_config.none": "无",
  "faction_config.allies": "盟军",
  "faction_config.soviet": "苏军",
  "faction_config.yuri": "尤里",
  "faction_config.save": "保存",
  "faction_config.close": "关闭",
  "deck_editor.title": "自定义初始卡组",
  "deck_editor.add_card": "添加卡牌",
  "deck_editor.restore_default": "恢复默认",
  "deck_editor.upgrade_level": "升级等级",
  "deck_editor.count": "数量"
}
```

---

## 5. 集成到 ModInitializer

[ModInitializer.cs](file:///d:/RedAlert2Project/red-alert-2-mod/RedAlert2ModCode/ModInitializer.cs) 无需大改：
- `harmony.PatchAll()` 会自动应用所有 `[HarmonyPatch]`（`CharacterFactionPatch`、`StartingDeckOverridePatch`、热键 Patch）。
- 若采用手动 Install 模式，追加：
  ```csharp
  CharacterFactionPatch.Install(harmony);
  StartingDeckOverridePatch.Install(harmony);
  ```

---

## 6. 验证清单

### 功能一
- [ ] 原版角色（如 Ironclad）配"盟军" → 开局弹出盟军国旗选择，获得 AlliedMCV + 刀乐
- [ ] 盟军角色配"盟军" → 无额外效果（已有 MCV/刀乐/国旗）
- [ ] 盟军角色配"苏军" → 注入 SovietMCV+刀乐+苏军国旗
- [ ] 重复开局不重复注入刀乐/MCV
- [ ] 联机下两端均生效，无 checksum 断连
- [ ] 配置文件删除后回退默认（全无）

### 功能二
- [ ] 列出全部角色，正确显示各角色默认初始卡组
- [ ] 编辑后保存，开局使用自定义卡组
- [ ] 升级等级正确应用（注入时循环 Upgrade）
- [ ] 恢复默认功能正常
- [ ] 与功能一叠加时 MCV 不重复注入
- [ ] 联机同步正确

### 通用
- [ ] 0 编译错误（允许既有 nullable 警告）
- [ ] 部署：复制 `build/RedAlert2Mod.dll` + `RedAlert2Mod.json` + `RedAlert2Mod.pck` 到游戏 mods 目录

---

## 7. API 待核实清单（实现前需确认）

| 项 | 说明 | 核实方式 |
|----|------|----------|
| `ModelDb.Characters` | 获取全部角色列表的属性名 | 反编译 sts2.dll 或查 STS2 API 文档；备选 `ModelDb.DebugCharacters` |
| `StartingDeckEntry` 字段 | `CardType`/`Count`/`UpgradeLevel` 实际字段名 | 读 `RestuLib/lib/0.109.0/STS2-RitsuLib.xml` |
| `YuriMCV` 卡牌类是否存在 | 尤里阵营是否可授予 MCV | 读 `Yuri/YuriCardRegistry.cs` |
| `RunState.CreateCard` 签名 | 创建卡牌到牌组的方法 | 已在 FreeLoadout `EmbeddedScreenPatches` 见到 `runState.CreateCard(cardModel, player)` |
| `CardPileCmd.Add(card, PileType.Deck)` | 加入牌组 | 已在 FreeLoadout 见到 `CardPileCmd.Add(card, pile)` |
| 原版角色初始卡组读取 | `CharacterModel` 是否有 `StartingDeck`/`CreateStartingDeck` | 反编译 sts2.dll 查 `CharacterModel` |
| `NGame.StartRun` 多 Postfix 顺序 | 功能一/功能二/FlagSelection 三者执行顺序 | 必要时用 `HarmonyPriority` 特性控制 |

---

## 8. 实现优先级建议

1. **先做功能一的配置存储 + FlagManager 扩展 + 开局注入**（核心闭环，无 UI 也能通过手改 JSON 验证）。
2. **再做功能一 UI 面板**（F6 唤出，列出角色选阵营）。
3. **再做功能二的配置存储 + 开局替换**（核心闭环）。
4. **最后做功能二 UI 面板**（F7 唤出，卡组编辑器，复杂度最高）。

每步独立可验证，避免一次性引入过多改动。
