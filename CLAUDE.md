# 识图能力（Vision Skill）

你的底层模型不具备原生识图能力。遇到图片时，**不要用 Read 工具**（Read 读不出图片内容），改用项目根目录的 vision.js 脚本识图：

```
node vision.js "<图片路径>" "请用中文描述这张图片的内容"
```

支持本地图片路径，也支持网络图片链接（加 `--url`）：

```
node vision.js --url "<图片URL>" "请用中文描述这张图片的内容"
```

## 触发场景（仅在这些情况调用，其他任务一律不用）

- 用户分享图片路径（本地或网络 URL）
- 消息中出现 "Saved attachments:" 并列出图片
- 用户要求分析、描述、识别、查看、读取图片内容
- 用户要求根据截图/图片判断界面、报错、设计等

## 强制规则

1. **仅当任务涉及图片/视觉内容时才运行 vision.js**。普通代码、文本、文件编辑任务绝不调用。
2. 识图一律走 `node vision.js`，由它调用已配置的识图模型（qwen3.7-flash），不要自行猜测图片内容。
3. 每张图片依次执行 vision.js，拿到所有文字描述后再回复。
4. 若 vision.js 报错，把完整错误信息反馈给用户，不要编造图片内容。

## 配置好之后

用户直接发图片，自动识图，无需手动打命令。
